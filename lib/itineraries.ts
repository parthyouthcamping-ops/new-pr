import { Quotation } from "./types";

export const PREDEFINED_QUOTES: Record<string, Quotation> = {
    // Hardcoded demo proposals have been moved to the database or removed.
    // New proposals added via the Admin Dashboard or API will appear instead.
};
